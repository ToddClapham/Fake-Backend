// ? is this defined correctly and is the file named correctly?
export enum SimState {
  Paused = 'paused',
  Resumed = 'resumed',
  Loading = 'loading',
  Unloading = 'unloading',
  PartiallyPaused = 'partiallypaused',
  PartiallyResumed = 'partiallyresumed'
}
